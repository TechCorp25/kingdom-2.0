# Session — studio-suite — Session 01 — Monorepo & toolchain baseline

Tier 3. Continuously updated: after each promoted task, at every decision, and
before any compaction. Promoted to project memory at session end.

## Intent

Stand up the REAL monorepo build so both apps compile and consume the shared
`@illuminate/tokens`. Foundation only — no product/feature work, no DS/visual
changes, no breaking changes to the existing Babel-standalone prototype.

## Goals file

`.orchestrator/projects/tasks/studio-suite/session-goals/2026-07-23T15-05-session-01-monorepo-baseline.md`
(roadmap: `.../session-goals/00-ROADMAP-first-sessions.md`)

Ordered goals: (1) pin toolchain (`.nvmrc`=22); (2) pnpm workspaces wiring, clean
`pnpm install` from root; (3) `@illuminate/tokens` buildable/consumable, `tsc
--noEmit` clean; (4) web Vite+React+TS skeleton alongside prototype (don't delete
it); (5) Expo scaffold boots + consumes shared tokens; (6) record baseline,
promote task→session.

## Branch

- Project repo: `session/2026-07-23-s01-monorepo-baseline` (off `main` @ 1bef7c0)
- Kingdom repo: `session/2026-07-23-studio-suite-s01-monorepo-baseline` (off `main` @ 2acda49)

## Decisions

- 2026-07-23 — Kingdom bootstrap: `main` fast-forwarded to `2acda49` (owner's #18
  close squash). Stale already-merged branch `session/2026-07-23-studio-suite-close`
  deleted (remote already gone; refs pruned). No remediation needed — last session
  (`2026-07-23T20-07`, seed-import) closed cleanly; SESSION.md was reset.
- Stray `memory-updater-temp-directory/` (cached Anthropic agent-memory API docs, not
  project content, untracked at repo root, not mine) moved to scratchpad — flagged to
  owner, out of the repo tree.
- Transient hook append-logs carried onto the kingdom session branch (commit at close).

## Progress

- Session bootstrapped; goals mirrored.
- Toolchain: Node 22.22.1 via nvm (installed; default was v24). pnpm 11.16.0 via
  corepack (`packageManager` pinned).
- Goal 1 ✓ `.nvmrc`=22.
- Goal 2 ✓ pnpm workspaces: `pnpm-workspace.yaml` (dropped npm `workspaces` field —
  pnpm 11 rejects it). Root scripts → pnpm filters. Fixes discovered/applied: apps use
  `workspace:*` (pnpm 11 no longer implicitly links `*`); esbuild build approved
  (pnpm 11 blocks build scripts); **pnpm 11 reads config from `pnpm-workspace.yaml`,
  not `.npmrc`** → moved `nodeLinker: hoisted` + `autoInstallPeers` there and removed
  `.npmrc` (was silently ignored → linker stayed `isolated`).
- Goal 3 ✓ `packages/tokens` tsconfig + `typecheck`; `tsc --noEmit` clean; imported by
  both apps.
- Goal 4 ✓ web Vite+React+TS skeleton at `apps/web/app/` (isolated from prototype at
  `apps/web/index.html`, which stays intact). `main.tsx` imports @illuminate/tokens.
  `vite build` OK (brand-gold `a8884f` in bundle); `vite dev` serves 200, resolves
  tokens from workspace source, serves the value. Added @types/react(-dom); scoped web
  tsconfig to skeleton.
- Fix (pre-existing bug): `apps/mobile/src/theme/useTokens.ts` exposed shadow key
  "gold" but `shadowToRN` only supports sm/md/lg (undefined → runtime crash); narrowed.
- Goal 5 ✓ `metro.config.js` (monorepo watchFolders + nodeModulesPaths). Under the
  hoisted linker, `expo export --platform android` bundles the full graph to Hermes;
  bundle string table contains App.tsx text + the entire token system → shared theme
  consumed. (First attempt failed on `@babel/runtime` under the isolated linker — root
  cause was the ignored `.npmrc`; fixed via `pnpm-workspace.yaml`.)
- Added `supportedArchitectures: current` so `pnpm install` only fetches current-platform
  binaries (avoids the rollup exotic-binary download hang).
- Goal 6 ✓ baseline recorded: `reports/sessions/2026-07-23T22-16-session.md`.
- **ALL 6 GOALS MET & VERIFIED.** Final: `pnpm -r typecheck` green ×3, `vite build` OK,
  `expo export` OK. Next: promote task→session, commit both layers, open PR (self-check
  parked — Fable out of credits).

## Promoted task outcomes

<!-- memory-promote.sh task appends here -->

### 2026-07-24T02-34 — Task — Session 01: monorepo & toolchain baseline — 2026-07-23
**Achieved:** Real pnpm-workspace monorepo baseline on Node 22.22.1 / pnpm 11.16.0.
All 6 goals met: (1) `.nvmrc`=22; (2) pnpm workspaces + clean `pnpm install`;
(3) `@illuminate/tokens` typechecks (`tsc --noEmit`) and is consumed by both apps;
(4) web Vite+React+TS skeleton at `apps/web/app/` (prototype at `apps/web/index.html`
left intact) — `vite build` + `vite dev` both render a token value; (5) Expo/Metro
bundles the mobile app consuming the shared theme (`expo export` → Hermes bundle
contains the full token system); (6) baseline recorded.

**Verified how:** `pnpm -r typecheck` green ×3; `pnpm --filter @illuminate/web build`
(brand-gold `#a8884f` in bundle) + `vite` dev server 200 with token module resolved;
`expo export --platform android` success + string-table inspection of the Hermes bundle.

**Key fixes:** `workspace:*` (pnpm 11 dropped implicit `*` linking); `nodeLinker:
hoisted` must live in `pnpm-workspace.yaml` not `.npmrc` (else Metro can't resolve
`@babel/runtime`); `allowBuilds: esbuild`; `supportedArchitectures: current`; and a
pre-existing runtime bug in `apps/mobile/src/theme/useTokens.ts` (`shadow("gold")`
would crash — narrowed the type).

**Follow-ups:** Session 02 = migrate prototype `src/features/*.jsx` into the Vite app.
Fable PR-readiness self-check parked (out of credits). Deprecated transitive subdeps
to revisit at next Expo SDK bump.
