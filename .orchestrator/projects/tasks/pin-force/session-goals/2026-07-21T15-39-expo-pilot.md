# Session plan — pin-force — 2026-07-21T15-39

<!-- Pilot session plan, seeded from the owner's build prompt §11 during the
     v2 suite build. The pilot session selects this file (latest for its day)
     or the owner supersedes it with a newer one. -->

## Goals (ordered)

1. Land the adoption housekeeping PR on the project repo: kingdom boundary
   `.gitignore` block (already applied locally, uncommitted).
2. Scaffold a basic **Expo React Native test application** as a workspace app
   (`apps/expo-test-app/`), respecting repo governance (no domain forks; app
   consumes shared capabilities only). Working = `npx expo start` renders the
   test screen; root `verify:structure` and `ci` scripts stay green.
3. Wire **EAS** build/deploy config (`eas.json`, profiles for preview +
   production) with all secrets externalized to gitignored env files.
4. Git workflow automations end-to-end: session branch → PR → checks → merge,
   exercised on goals 1–3 as real PRs on `CivicMAPS/pin-force`.
5. **EAS deployment** of the test app.

## Success criteria

- Expo app boots locally (Metro starts; test screen renders).
- Project PRs opened with checks green; boundary-verify.sh all ✓ throughout.
- Task/session memory promoted per DD-002; every gate decision recorded.
- Dry-run close: pin-force code landed only in `CivicMAPS/pin-force`;
  pin-force memories/reports landed only in the kingdom repo.

## Constraints

- Foundation governance untouched: nothing under `docs/governance/approved-source/`
  changes; no runtime/hosting decisions asserted beyond the test app itself.
- No secrets in either repo layer at any point.

## Gate notes

- **HARD (owner):** Expo/EAS API keys and tokens — request at goal 5, store
  only in gitignored env; the deployment itself is owner go/no-go.
- **Self-check (Fable):** project PR merges for goals 1–4 once the owner has
  ratified the gate framework (kingdom PR #5); until then project merges park
  as HARD pending owner, same as the kingdom stack.
- **Go:** each goal's success criteria above.
