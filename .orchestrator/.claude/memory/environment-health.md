# Environment health

Updated by the kingdom-bootstrap workflow and session-end hook. Keep entries
current; prune anything stale.

## Machine + tooling

- Linux 6.6 Crostini · git, gh (authed), node, python3, uv present · no jq
  (scripts use python3 for JSON).

## Sync status

- 2026-07-21 — kingdom remote `TechCorp25/kingdom-2.0` created; build in
  progress on stacked `build/*` branches.

## Package currency

- No suite runtime dependencies yet (pure bash + python3 stdlib by design).
