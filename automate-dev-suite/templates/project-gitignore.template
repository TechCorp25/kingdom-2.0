# ── Kingdom boundary (generated by automate-dev-suite — do not remove) ──
# State syncs through the KINGDOM repo, never this one (DD-001).
.orchestrator
.claude

# ── Secrets — never in either repo layer ────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
**/secrets/

# ── Dependencies / build output ─────────────────────────────────────────
node_modules/
dist/
build/
.expo/
__pycache__/
*.pyc
.venv/

# ── OS / editor noise ───────────────────────────────────────────────────
.DS_Store
*.swp
*~
